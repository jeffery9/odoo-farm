# 农业套件 107 模块依赖关系全景审计 (Dependency Panorama)

*版本: V1.0 | 审计日期: 2026-05-04*

## 1. 宏观拓扑概览 (Topological Overview)

本项目包含 107 个物理微服务模块，采用**分层有向无环图 (Layered DAG)** 架构设计。

- **依赖环路状态**: ✅ **Green (无环)**。全局审计未发现 (A->B->C->A) 形式的循环依赖，确保了安装过程的幂等性。
- **架构最大深度**: **12 层**。
- **核心依赖枢纽 (Top 3 Hubs)**:
    1.  `farm_core` (79 个模块依赖): 定义了地块、品种、GIS 等农业通用原子数据。
    2.  `farm_operation` (37 个模块依赖): 驱动农事干预与任务计划的核心引擎。
    3.  `farm_agri_science` (12 个模块依赖): 提供积温、生理阶段等农学智能计算。

## 2. 架构分层映射 (Architectural Layers)

系统由底向上分为 13 个逻辑层级，每一层仅依赖于其下方的层级，实现了完美的横向解耦。

### 层级 L0: 系统底座 (Foundation)
- **关键模块**: `farm_core`, `agri_iot`, `farm_ux`
- **职责**: 定义物理资产模型（Asset Model）、IoT 协议基准、以及全站统一的“去工业化”UX 术语转换。

### 层级 L1-L3: 业务垂直底座 (Vertical Foundations)
- **关键模块**: `farm_esg`, `farm_supply_core`, `farm_agri_science`, `precision_production`
- **职责**: 建立各垂直领域（ESG、供应链、农学、精密制造）的抽象 Mixin 和逻辑基准。

### 层级 L4-L6: 生产与操作执行层 (Execution & Operations)
- **关键模块**: `farm_operation`, `farm_land_mgmt`, `farm_crop`, `farm_hr`, `farm_isl`
- **职责**: 处理具体的农田作业、人力排班、以及行业标准层 (ISL) 的代理分发。

### 层级 L7-L9: 高级行业垂直应用 (Industry Specializations)
- **关键模块**: `farm_livestock`, `farm_aquaculture`, `farm_processing`, `farm_ai_vision`
- **职责**: 针对畜牧、水产、深加工、以及 AI 视觉分析等特定行业的精细化业务闭环。

### 层级 L10-L12: 智能决策与全球合规顶层 (Intelligence & Compliance)
- **关键模块**: `farm_ai_agent`, `farm_esg_compliance`, `farm_green_monitor`
- **职责**: 全局智能体协同、碳中和审计报告、以及跨境出口合规硬拦截。

## 3. 演进建议 (Recommendations)

1.  **Hub 稳定性保护**: 由于 `farm_core` 的变更会影响 79% 的模块，后续针对该模块的 `replace` 操作必须执行最严苛的单元测试覆盖。
2.  **解耦深层链**: 层级 L10 以上的模块依赖链过长（Depth > 10），在 Odoo 19 中加载时应优先检查 `precompute=True` 的应用，以防止跨模块计算字段产生的层叠数据库查询压力。
3.  **循环依赖预警**: 在后续开发中，禁止在 `farm_core` 中直接引用高层模块（如 `farm_ai_agent`），必须通过 `AbstractModel` 或 `Selection` 动态注入。

---
*Generated by Dependency Analyzer Engine - 2026-05-04*
