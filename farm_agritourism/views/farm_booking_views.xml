<?xml version="1.0" encoding="utf-8"?>
<odoo>
<data>
    <!-- Booking Views [US-005-03] -->
    <record id="farm_booking_view_calendar" model="ir.ui.view">
        <field name="name">farm.booking.calendar</field>
        <field name="model">farm.booking</field>
        <field name="arch" type="xml">
            <calendar string="Farm Bookings" date_start="date_start" date_stop="date_stop" color="resource_id" mode="week">
                <field name="partner_id"/>
                <field name="resource_id"/>
                <field name="booking_type"/>
            </calendar>
        </field>
    </record>

    <record id="farm_booking_view_kanban" model="ir.ui.view">
        <field name="name">farm.booking.kanban</field>
        <field name="model">farm.booking</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_mobile" default_group_by="state">
                <field name="name"/>
                <field name="partner_id"/>
                <field name="date_start"/>
                <field name="booking_type"/>
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_global_click">
                            <div class="o_kanban_record_top">
                                <strong class="o_kanban_record_title"><field name="partner_id"/></strong>
                                <span class="badge rounded-pill bg-info float-end"><field name="booking_type"/></span>
                            </div>
                            <div class="o_kanban_record_body">
                                <div>Ref: <field name="name"/></div>
                                <div class="text-muted"><i class="fa fa-calendar"/> <field name="date_start"/></div>
                            </div>
                            <div class="o_kanban_record_bottom">
                                <field name="state" widget="label_selection" options="{'classes': {'draft': 'default', 'confirmed': 'success', 'done': 'primary'}}"/>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="farm_booking_view_pivot" model="ir.ui.view">
        <field name="name">farm.booking.pivot</field>
        <field name="model">farm.booking</field>
        <field name="arch" type="xml">
            <pivot string="Booking Analysis">
                <field name="booking_date" interval="month" type="col"/>
                <field name="booking_type" type="row"/>
                <field name="resource_id" type="row"/>
            </pivot>
        </field>
    </record>

    <record id="farm_booking_view_tree" model="ir.ui.view">
        <field name="name">farm.booking.list</field>
        <field name="model">farm.booking</field>
        <field name="arch" type="xml">
            <list string="Farm Bookings">
                <field name="date_start"/>
                <field name="name"/>
                <field name="partner_id"/>
                <field name="resource_id"/>
                <field name="state" widget="badge"/>
            </list>
        </field>
    </record>

    <record id="farm_booking_view_form" model="ir.ui.view">
        <field name="name">farm.booking.form</field>
        <field name="model">farm.booking</field>
        <field name="arch" type="xml">
            <form string="Farm Booking">
                <header>
                    <button name="action_confirm" string="Confirm" type="object" class="btn-primary" invisible="state != 'draft'"/>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1><field name="name" placeholder="Booking Ref..."/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="partner_id"/>
                            <field name="booking_type"/>
                            <field name="resource_id" invisible="booking_type != 'resource'"/>
                        </group>
                        <group>
                            <field name="date_start"/>
                            <field name="date_stop"/>
                            <field name="land_parcel_id" invisible="booking_type == 'resource'"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Resource Views -->
    <record id="farm_resource_view_tree" model="ir.ui.view">
        <field name="name">farm.resource.list</field>
        <field name="model">farm.resource</field>
        <field name="arch" type="xml">
            <list string="Farm Resources" editable="bottom">
                <field name="name"/>
                <field name="resource_type"/>
                <field name="active"/>
            </list>
        </field>
    </record>
</data>
</odoo>
