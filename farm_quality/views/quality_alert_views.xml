<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="farm_quality_alert_view_tree" model="ir.ui.view">
        <field name="name">farm.quality.alert.list</field>
        <field name="model">farm.quality.alert</field>
        <field name="arch" type="xml">
            <list string="Quality Alerts" decoration-danger="priority == '2'" decoration-warning="state == 'new'">
                <field name="name"/>
                <field name="lot_id"/>
                <field name="user_id"/>
                <field name="priority" widget="priority"/>
                <field name="state" widget="badge"/>
            </list>
        </field>
    </record>

    <record id="farm_quality_alert_view_form" model="ir.ui.view">
        <field name="name">farm.quality.alert.form</field>
        <field name="model">farm.quality.alert</field>
        <field name="arch" type="xml">
            <form string="Quality Alert">
                <header>
                    <button name="action_confirm" string="Confirm" type="object" class="btn-primary" invisible="state != 'new'"/>
                    <button name="action_close_scrapped" string="Mark as Scrapped" type="object" class="btn-secondary" invisible="state == 'closed'"/>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1><field name="name"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="lot_id"/>
                            <field name="product_id"/>
                            <field name="check_id" readonly="1"/>
                        </group>
                        <group>
                            <field name="user_id"/>
                            <field name="priority" widget="priority"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <field name="description" placeholder="Describe the quality issue..."/>
                        </page>
                        <page string="Corrective Actions">
                            <group>
                                <field name="cause"/>
                                <field name="action_taken"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <record id="action_farm_quality_alert" model="ir.actions.act_window">
        <field name="name">Quality Alerts</field>
        <field name="res_model">farm.quality.alert</field>
        <field name="view_mode">list,form,kanban</field>
    </record>

    <menuitem id="menu_quality_alert" name="Quality Alerts" parent="menu_quality_root" action="action_farm_quality_alert" sequence="30"/>
</odoo>
