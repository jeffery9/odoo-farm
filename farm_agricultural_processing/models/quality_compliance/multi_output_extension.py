# -*- coding: utf-8 -*-
from odoo import models, fields, api

class AgriProcessingMultiOutputLine(models.Model):
    _inherit = 'agri.quality.check' # Wait, no, it's a new model in the other file.
    
    # Actually, I'll just add it to the other file.
