<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- 表单布局模板视图 -->
    <record id="view_form_layout_template_tree" model="ir.ui.view">
        <field name="name">form.layout.template.tree</field>
        <field name="model">form.layout.template</field>
        <field name="arch" type="xml">
            <tree string="Form Layout Templates" decoration-danger="not is_active" decoration-success="is_active">
                <field name="name"/>
                <field name="model_name"/>
                <field name="industry_type"/>
                <field name="user_role"/>
                <field name="version"/>
                <field name="is_active"/>
            </tree>
        </field>
    </record>

    <record id="view_form_layout_template_form" model="ir.ui.view">
        <field name="name">form.layout.template.form</field>
        <field name="model">form.layout.template</field>
        <field name="arch" type="xml">
            <form string="Form Layout Template">
                <header>
                    <field name="is_active" widget="boolean_toggle"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1><field name="name" placeholder="Template Name"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="model_name"/>
                            <field name="industry_type"/>
                            <field name="user_role"/>
                        </group>
                        <group>
                            <field name="version"/>
                            <field name="created_by"/>
                            <field name="created_date"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Layout Definition">
                            <field name="layout_definition" widget="ace" options="{'mode': 'json'}"/>
                        </page>
                        <page string="Description">
                            <field name="description" nolabel="1"/>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- 表单布局模板菜单和动作 -->
    <record id="action_form_layouts" model="ir.actions.act_window">
        <field name="name">Form Layout Templates</field>
        <field name="res_model">form.layout.template</field>
        <field name="view_mode">tree,form</field>
    </record>

    <menuitem id="menu_form_layouts" 
              name="Form Layout Templates" 
              parent="farm_ux_deindustrialization.menu_ux_root" 
              action="action_form_layouts" 
              sequence="20"/>
</odoo>